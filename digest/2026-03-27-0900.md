# Daily AI Digest — 2026-03-27

> *HN pulse as of 09:00 CST (01:00 UTC)*

---

## 🔥 The Headline

**LiteLLM 1.82.8 was a supply chain attack, and an ML engineer caught it in 46 minutes using Claude Code.** The attack vector — a `.pth` file injected via a stolen CI token into a transitive dependency (`futuresearch-mcp-legacy → litellm`) — is a canonical template for MCP-era supply chain risk. Every team with an AI toolchain dependency on PyPI is now on notice.

---

## 📡 The Stack

**[Breakout] My minute-by-minute response to the LiteLLM malware attack** — On March 24, litellm v1.82.8 was pushed to PyPI with a malicious `.pth` file that exfiltrates to `models.litellm.cloud` via RSA and installs on every Python startup (no import needed). The package was live for 46 minutes. A dev caught it via an 11k-process fork bomb while using Cursor, then used Claude Code to do forensics and coordinate disclosure — all in one session. The implications: `.pth`-based persistence is nastier than `postinstall` hooks, the MCP dependency graph is an unexplored attack surface, and LLM-assisted incident response is now real. Dep cooldowns (uv's `--exclude-newer` pattern) just became standard hygiene. [285 pts]

**[Agents] HyperAgents: Self-referential self-improving agents** (Meta AI Research) — arXiv:2603.19461. A framework where a *meta-agent* rewrites task agents to improve performance on any computable domain, then evaluates the diff. It's a working implementation of recursive self-improvement in a sandboxed loop, with code and experiment logs published. The architecture separates the optimizer from the executor cleanly — this is the right structural move. Ships with Docker and multi-API support (OpenAI, Anthropic, Gemini). The safety disclaimer ("may behave destructively") is appropriately honest. [128 pts]

**[Agents] From zero to a RAG system: successes and failures** — A rare production post-mortem on building RAG over 451 GB of heterogeneous engineering documents, from scratch, by an engineer who had never done it before. Key failure modes documented: LlamaIndex's in-memory JSON index dies on multi-hundred-GB corpora; ChromaDB + SQLite with batch checkpointing was the fix; GPU is the real bottleneck for embedding at scale (€184 on Hetzner for a 2-3 week index run); and PPTX/image-heavy content is still basically unsolved. Dense with concrete engineering decisions. [284 pts]

---

## 🗑️ The Noise

Nothing on the front page today qualifies as AI hype worthy of calling out — the Anthropic/Pentagon injunction story (blocked supply chain risk label) is policy news with zero technical content, even if the implications for Anthropic's enterprise contracts are non-trivial.

---

## 🌡️ Vibe Check

The community is watching AI tooling eat its own tail — both the attack and the defense in today's top story ran on the same LLM stack, and nobody missed the irony.
