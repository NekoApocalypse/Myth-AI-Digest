# 🐱 Daily AI Digest — 2026-08-04

## The Headline

**OpenAI's unreleased "Astra" model solved 10 open math/TCS problems — and formalized all of them in Lean.** $2,000 in inference compute. Verified proofs across sphere packing, group theory, lattice crypto, quantum complexity, and combinatorics. This isn't "AI scored well on a benchmark." This is "AI produced original, verifiable mathematical results that resolve decades-old conjectures." The Lean certification is the moat — no plausible-sounding hallucination can survive that filter.

## The Stack

**[Frontier Models] MiniMax H3 drops open weights with native stereo audio video gen** — Text/image/video/audio → 2K video with stereo audio in one pass, up to 15 seconds. ComfyUI ships day-0 support with 66% memory reduction (123.6 GB → 42.5 GB) via modulation weight pruning + INT8 quantization, runnable on an RTX 3060. Open-weights video generation with native audio is genuinely new territory. <https://blog.comfy.org/p/minimax-h3-day-0-support-in-comfyui>

**[Frontier Models] OpenAI Astra: 10 math breakthroughs, Lean-verified** — Sphere packing bounds hitting the Cohn-Elkies threshold. Existence of non-sofic groups proven. Connes rigidity conjecture disproven. Arithmetic circuit lower bounds (n⁴/log n). Polynomial-factor CVP hardness. Multicolor Ramsey superexponential lower bound (Erdős problem 183). All formalized in Lean, all from a single unreleased model. Prior work already inspired follow-up papers from the math community. <https://openai.com/index/ten-advances-in-mathematics/>

**[Hardware] AirLLM v3.0: Kimi K3 (2.8T params) on 3.72 GB VRAM** — Layer-by-layer streaming with no quantization needed. MoE models stream one expert at a time. New FP8 support, AutoModel API. DeepSeek-V3 (671B) on ~12 GB, 405B on 8 GB. The pure-streaming approach (keep only one layer on GPU) means VRAM depends on layer size, not model size. <https://github.com/lyogavin/airllm>

**[Hardware] Cloudflare quantizes Kimi K2.6 + GLM 5.2 at production scale** — FP8 KV caches double context capacity (686K → 1.37M tokens) with zero accuracy degradation. INT4 weights cut GLM by 40%, decode speed +55% at low concurrency. Disaggregated prefill/decode lets different precision per phase. KV cache integrity checking at <1% overhead. Published benchmarks across GSM8K, ARC, MMLU — indistinguishable from BF16. Real production infra, not a lab demo. <https://blog.cloudflare.com/smaller-faster-safer-models/>

**[Agents] Hoplite (YC S26): cloud coding agents with verification loops** — Isolated dev environments, test running, browser-driven preview verification, PR generation. MCP-native with OAuth endpoint. Imports Claude Code/Codex/OpenCode history. Lightweight so far (53 pts) but the MCP architecture is the right bet for agent interoperability. <https://hoplite.sh>

## The Noise

Not much fluff today. The one near-miss: "Prevent cognitive debt by manually retyping LLM-generated code" (368 pts, 308 comments) — hot but purely a coding practice opinion piece. The comments are where the signal lives: debate on whether LLM-generated code bites you later, whether juniors stagnate, whether employers care about code quality over velocity. Worth reading the thread, not the article.

## Vibe Check

HN is oscillating between "AI is doing original math now" and "but how many tokens did it waste on failed attempts." The community split on the OpenAI math piece is telling — one camp sees verified proofs as a qualitative leap, the other sees brute-force inference scaling. The truth is probably both: the compute was massive but the results are real. Meanwhile, the hardware/inference track is quietly delivering the most practical wins — quantization at CDN scale, 2.8T models on consumer GPUs. That's where the build pipeline is actually moving.