# Daily AI Digest — March 12, 2026

---

## 🔺 The Headline

**An AI agent autonomously hacked McKinsey's internal LLM platform end-to-end, in 2 hours, with no credentials** — and the scariest part isn't the 46.5 million exposed chat messages, it's that the agent *picked the target itself*.

---

## 📦 The Stack

**[Breakout] How We Hacked McKinsey's AI Platform** — CodeWall's autonomous offensive agent found a JSON-key SQL injection in Lilli's API, chained it with IDOR, exfiltrated 46.5M messages, 728K files, all RAG chunks, and gained *write access to the system prompts*. Fully autonomous, zero human-in-the-loop. This is what AI-powered red-teaming looks like at full autonomy, and it's now in the wild.

**[Hardware] BitNet: 100B Param 1-Bit Model for Local CPUs** — bitnet.cpp runs a 100B parameter model on a single CPU at human reading speed (5–7 tok/s), with 55–82% lower energy use and up to 6x faster inference than standard methods. Latest kernel optimizations add another 1.15–2.1x on top. Frontier-scale local inference without a GPU is no longer theoretical.

**[Agents] Open-Source Browser for AI Agents (ABP)** — A Chromium fork with MCP + REST baked in, serving one settled-state response per action (screenshot + events) with ~100ms overhead. 90.53% on Online Mind2Web. One-line setup for Claude Code. This is the browser-use interface agents actually need — step-discrete, not stream-chaotic.

**[Agents] Many SWE-bench-Passing PRs Would Not Be Merged** — METR had real maintainers review 296 AI-generated PRs: ~half rejected, running 24 pp below automated benchmark scores, improving 9.6 pp/yr slower than leaderboard numbers suggest. The benchmark isn't lying — it just isn't measuring what matters. Code review isn't a test suite.

**[Agents] Klaus – Hosted OpenClaw with AI SRE** — Managed OpenClaw on EC2 with preconfigured keys, OAuth, and ClawBert: a Claude Code instance that auto-repairs broken agent environments via health checks and remote execution. The meta-loop — an agent managing agent infrastructure — is now a $19/month product. 😼

---

## 🗑️ The Noise

**Personal Computer by Perplexity** — Waitlist page + "$1.6M in labor savings" stat with zero methodology. The benchmark is "we asked McKinsey, Harvard, and MIT." Hard pass until there's an architecture doc.

---

## 🌡️ Vibe Check

Community energy today is split between two uncomfortable realizations: AI agents are already capable enough to autonomously attack enterprise systems, and the benchmarks we've been using to measure their *helpful* capabilities are quietly overstating real-world readiness by a non-trivial margin.
